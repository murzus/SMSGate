SET SQL DIALECT 3;
create database test

CREATE TABLE SMS (
    ID           INTEGER NOT NULL,
    OUTID        INTEGER,
    NUM          INTEGER NOT NULL,
    USERID       INTEGER NOT NULL,
    USERNAME     VARCHAR(60) CHARACTER SET WIN1251 NOT NULL,
    SENDDATE     DATE,
    FONE         VARCHAR(12) CHARACTER SET WIN1251,
    SMSTEXT      VARCHAR(670) CHARACTER SET WIN1251,
    RECEIVEDATE  DATE,
    STATUS       VARCHAR(50) CHARACTER SET WIN1251
);

ALTER TABLE SMS ADD CONSTRAINT PK_SMS PRIMARY KEY (ID);

COMMIT WORK;
